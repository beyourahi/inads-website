import type { NextPage } from "next";
import { Layout } from "../components";

const Services: NextPage = () => (
    <Layout page="Services">
        <h1 className="text-3xl font-bold text-center">Services Page</h1>
    </Layout>
);

export default Services;

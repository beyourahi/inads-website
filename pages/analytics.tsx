import type { NextPage } from "next";
import { Layout } from "../components";

const Analytics: NextPage = () => (
    <Layout page="Analytics">
        <h1 className="text-3xl font-bold text-center">Analytics Page</h1>
    </Layout>
);

export default Analytics;
